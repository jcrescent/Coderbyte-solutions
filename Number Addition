function NumberAddition(str) { 

  var nums=str.match(/[0-9]+/g);
  var total=0;
  
  if(nums!=null) {
    for(var i=0;i<nums.length;i++) {
      total+=nums[i]*1;
     }
   }
  return total; 
        
}
