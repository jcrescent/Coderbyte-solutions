function SquareFigures(num) { 
  var number=num.toString();
  var arr=[];
  
  for (var i=0; i<Math.pow(10,(num+1));i++) {
    if ((i*i).toString().length==num)
      arr.push(i);
  }
    return arr[0];
}
